# LLM プロバイダ: claude | openai | mock
# mock は API キー・実 PDF 不要で配管サンプルを返す（疎通確認用）
GOPIPE_LLM_PROVIDER=mock

# 本番で Claude を使う場合のみ設定（チャットに貼らず .env / シークレット管理で）
ANTHROPIC_API_KEY=

# OpenAI を使う場合のみ
OPENAI_API_KEY=

# ログレベル（DEBUG / INFO / WARNING）
GOPIPE_LOG_LEVEL=INFO
