# Vercel Python (api/index.py) サーバレス関数用の依存。API に必要な最小限のみ。
# streamlit / pandas / uvicorn / openai は API 実行に不要なのでバンドルから除外。
# ローカル全機能は pyproject.toml ("[web]" "[api]" extras) を使うこと。
fastapi>=0.110.0
python-multipart>=0.0.9
anthropic>=0.40.0
pymupdf>=1.24.0
openpyxl>=3.1.2
pydantic>=2.7.0
pyyaml>=6.0.1
